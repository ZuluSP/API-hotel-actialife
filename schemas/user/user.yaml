type: object
description: user of mf bussiness money money

allOf:
  - $ref: "./usercreate.yaml"

  - properties:
      userId:
        $ref: "../generic/Id.yaml"

    required:
      - userId
      - name
